# Gradle / Build System
.gradle/
build/
app/build/
local.properties

# Android build outputs
*.apk
*.aab
*.dex
*.class
*.iml

# Intermediates（大量に生成されるので除外）
app/build/intermediates/
app/build/outputs/
app/build/tmp/

# IDE / Editor
.idea/
*.log

# Keystore（絶対に公開しない）
sobatimer.keystore

# app/build.gradle（公開しない）
app/build.gradle
